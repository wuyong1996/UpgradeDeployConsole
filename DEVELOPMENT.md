# 服务器更新管理开发说明

> 2026-09-24 最新交付保留规则：用户明确要求生成部署包后删除全部旧交付。统一Package-DeployConsole.ps1先完成构建/完整性核验，再清理旧包、解压目录和旧附件，仅保留最新压缩包/升级脚本/DEPLOY.md；失败保留原可用包。此约定覆盖历史文档中保留本地旧包的做法，不改变服务器已安装版本或数据库恢复备份策略。

- 独立管理系统：ASP.NET Core 10、Vue 3 / TypeScript、Python标准库与Linux systemd / Nginx适配器。
- `src/DeployConsole.Api`保存API、认证、项目配置、作业队列、调度和审计；状态使用受保护JSON文件，面板自身无需MySQL。
- `src/frontend`构建到API的`wwwroot`；先构建页面再构建API，避免静态资源索引过时。
- `deploy/console`提供安装/升级、Git拉取、服务发布、MySQL项目库、HTTPS和删除功能。root适配器仅处理已核对的独立项目资源。
- `tests/DeployConsole.Tests`、`tests/scripts`只依赖本仓库；Linux服务/MySQL命令使用mock，HTTP测试用临时目录及回环端口。
- 业务工程通过自己的`deploy-console.json`及迁移命令接入，迁移代码应提交到对应业务仓库。本面板不会引用TrainingRegistration程序集。
- SDK、NuGet和npm依赖版本在本仓库配置与锁文件中固定。无子模块、无指向旧工程的源码链接。
- API详情、安装与工程接入见[运维说明](docs/deploy-console.md)和[接入规范](docs/deploy-console-project-onboarding.md)。

## 历史决策

下列内容按原记录保留；涉及旧目录/TrainingRegistration的条目是历史背景，当前布局以上文和README为准。

> 2026-09-24 OPS-01 可选不备份删除：按用户明确要求，删除弹窗默认备份，可取消并另行确认不生成恢复备份。API/root双端验证布尔选项与确认，旧请求/任务默认备份；任务、审计和主机阶段记录保存选择。不备份跳过文件归档及mysqldump，不保存数据库密码，只保留重试归属元数据；所有停机/资源/连接检查继续执行。preparing阶段可改选，进入清理后固定原选择，结果和历史明确无本次恢复备份；不删除独立保存的删除恢复备份。

> 2026-09-24 OPS-01 升级缺包跳过：用户最新明确取消读取.sha256，覆盖上一版必须校验的流程。独立upgrade.sh支持缺包跳过安装，正常升级或现有面板健康后核对同版解压内容与当前安装的DLL/页面/适配器，清理已确认的本次/遗留随机目录和直接上传目录。非正常路径、不同版本或内容、外层额外文件及挂载点保留；校验文件仅可选删除，不读取。缺包跳过不标记为升级成功，前置失败保留材料。

> 2026-09-24 OPS-01 升级上传材料清理：用户要求每次升级后删除升级包。外层升级命令按校验→本次独立临时目录解压→安装→HTTP/JSON健康确认→临时目录边界校验→清理顺序执行，仅删除本次精确上传包、校验文件和mktemp目录。失败保留排查材料，安装器不猜测归档路径，当前运行版本、历史安装版本、业务资源与恢复备份不在清理范围；不以通配符批量清理历史上传目录。

> 2026-09-24 OPS-01 旧版删除识别兼容：20260923自动接入产物未写入项目级/前端autoManaged，缺少字段不能直接等同人工接入。删除预览允许已有自动后端标记的旧格式，经原生成服务标记、专属账号及运行目录证明后继续完整资源检查；显式false或缺少自动创建证据仍拒绝，不修改接入文件或自动提升资源归属。部分删除重试只在受保护记录为clearing时允许已清理的服务文件缺失。界面拒绝区说明需满足的条件与接入文件位置，API契约及删除确认不变。

> 2026-09-24 OPS-01 资源信息与项目删除：用户明确要求总关停后删除项目及关联资源。数据库状态新增名称/地址/账号/迁移摘要，不返回凭据；界面增加Git源码目录、服务单元及开机启动状态。GET `/api/v1/projects/{id}/deletion`返回服务器计算的精确范围/指纹/备份目录；jobs新增delete，要求总关停、空闲、版本、幂等键、精确标识及预览指纹确认。删除意图持久化后禁止修改/启动/发布；root仅清理由面板自动创建的独立资源，校验归属、停机、挂载/链接和数据库账号，再备份与分阶段清理。SQL执行结果不确定时拒绝重放，已删库后的文件清理重试不再删库；完成后移除项目及加密Git凭据，保留历史/审计/恢复备份。共享MySQL、Nginx、全局证书及无关项目保留，详见[删除升级说明](docs/deploy-console-delete-upgrade.md)。

> 2026-09-24 OPS-01 ACME路径兼容修正：HTTPS设置的.NET/Python路径规则同步允许实际文件名中的字面量星号，例如`/root/.acme.sh/*.halfsuger.top_ecc/fullchain.cer`和同目录的`*.halfsuger.top.key`。仍按精确路径读取，不执行glob或shell；具体访问域名、PEM内容、密钥匹配和root权限校验保持不变，修正不改变公共接口或业务数据库。

> 2026-09-24 OPS-01 默认域名与HTTPS：按用户确认读取服务器已有ACME完整证书链和私钥路径，新增受会话/CSRF保护的GET/PUT `/api/v1/settings/https`，不执行证书签发。root适配器验证文件权限、域名覆盖、有效期、系统信任及密钥匹配，保存受保护快照；重写本面板已管理的Nginx前端站点，检查并重载，失败还原配置。前端沿用原端口并返回`accessUrl`，本机健康检查使用域名SNI及正常信任验证；后端继续回环HTTP。systemd timer每15分钟检查源文件变化并同步，新项目继承配置，停用项目保持503。管理面板自身入口和业务认证不变，升级及实机验证见[说明](docs/deploy-console-https-upgrade.md)。

> 2026-09-24 OPS-01 自动端口：新接入前后端从15000–25000（含边界）顺序分配；先排除target和主机state保留端口，再探测IPv4/IPv6能否绑定。已停止及手动改端口的服务仍保留配置，已有服务端口不迁移；范围用尽或探测异常时报错，接入分配仍受全局操作锁保护。

> 2026-09-24 OPS-01 总开始恢复：项目级jobs增加兼容的start-all操作，全停时切换总按钮；先恢复数据库访问，待初始化或缺少后端版本时复用既有发布/迁移流程，再启动后端、前端。成功后清除停止标记但保持自动发布暂停。请求入队时间、短时意图锁和项目版本共同保护后续关停优先；未确认的迁移阶段阻止自动重放。首次未绑定项目继续使用恢复并发布，普通发布仍遵守单端停止。详见[升级说明](docs/deploy-console-start-all-upgrade.md)。

> 2026-09-24 OPS-01 自动数据库与前端补接：按用户确认复用宝塔MySQL；新增受认证/CSRF保护的GET/PUT `/api/v1/settings/mysql`，一次授权管理连接，凭据仅由root适配器保存。prepare重读仓库描述补齐缺失端，保留服务端口和本地运行设置。database描述驱动固定建库/独立运行与迁移账号、自动连接注入、升级前备份及非root迁移协议；业务API增加离线plan/apply命令，复用既有管理员初始化，检查精确目标、历史前缀与摘要，冲突失败保留现场。自动项目库总关停锁定项目账号、不停止共享实例；未声明database的项目保持原接入方式。业务HTTP认证、会话、CSRF和既有迁移文件不变，详见[接入规范](docs/deploy-console-project-onboarding.md)。

> 2026-09-23 OPS-01 保存后自动部署：项目写入新增可选`deployAfterSave`和`enableSchedule`，状态与prepare任务原子入队；旧请求默认仍只保存。root-owned自动接入策略限定Git来源，固定模板以隔离账号拉取、识别npm静态前端/.NET Web、分配空闲端口并生成独立接入文件和systemd服务；已有接入继续复用。成功接入后按后端→前端发布，前置失败取消后续任务，停止优先且成功后才按选择启用计划；`publish`作完整流程重试。Nginx适配标准及宝塔默认路径，仅创建独立文件。仓库`deploy-console.json`仅声明目录/程序集/健康路径；数据库、运行密钥、系统依赖仍由服务器现有配置提供，不自动迁移或修改业务库。操作流程和兼容性见运维文档。

> 2026-09-23 OPS-01 Git分支与认证：新增受会话/CSRF保护的`POST /api/v1/git/branches`，保存前从远端选择分支并绑定10分钟查询凭证。项目输入新增可选`gitAuth`和`branchVerification`，旧调用兼容；公开项目DTO仅增加认证方式和用户名。凭据使用Data Protection加密并绑定仓库、服务器HMAC保护请求摘要；Linux检查/clone通过stdin及systemd LoadCredential临时交付，构建命令不携带秘密。默认查询主机白名单及详细接口见运维文档；不改变业务系统身份认证或数据库。

> 2026-09-23 OPS-01 添加项目交互：弹窗遮罩不触发关闭；名称失焦时本地生成不超过24字符的标识，中文拼音首字母、英文规范化及重名序号，手填标识和目录保留，编辑现有项目不改变标识。运维前端增加锁定依赖`tiny-pinyin 1.3.2`及随包许可证；API及服务接入规则不变。

> 2026-09-22 OPS-01 通用服务器更新管理：按用户确认的独立运维面板设计，在 `src/operations` 增加 .NET 10 / Vue 3 应用；使用独立域名、进程、服务器文本密码和原子 JSON 状态。它管理多项目 Git 来源、前后端独立计划、运行目录/端口与数据库关停，不改变本系统的业务 API、身份规则或 MySQL 表。Linux 真实操作通过 root 审核的项目接入文件和固定适配器执行，构建使用隔离的非 root 账号。安装、接口与上线边界见 [通用更新管理说明](docs/deploy-console.md)，本地验证证据见 [验证记录](docs/deploy-console-validation.md)。
